{
  "whitelist": {
    "enabled": true,
    "ips": [
      "127.0.0.1",
      "::1"
    ],
    "comment": "Whitelisted IPs will never be blocked. Internal IP ranges are automatically whitelisted without requiring configuration."
  },
  "blocking": {
    "enabled": true,
    "tempBlockDuration": 3600000,
    "maxViolationsBeforeTempBlock": 50,
    "maxTempBlocksBeforePermanent": 10,
    "violationWindow": 300000,
    "violationDecayTime": 1800000,
    "comment": "Blocking configuration: temporary block for 1 hour, 50 violations within 5 minutes trigger temporary block, 10 temporary blocks trigger permanent block"
  }
}
